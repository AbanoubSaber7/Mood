dependencies:
  shared_preferences: ^2.2.2
  flutter:
    sdk: flutter
  fl_chart: ^1.2.0                # updated
  tflite_flutter: ^0.12.1
  image: ^4.1.3
  camera: ^0.12.0+1              # updated
  cupertino_icons: ^1.0.8
  http: ^1.6.0
  url_launcher: ^6.3.2
  image_picker: ^1.2.1
  file_picker: ^11.0.2           # updated
  crypto: ^3.0.7
  firebase_core: ^4.7.0          # update if resolver suggests
  firebase_auth: ^6.4.0          # update if resolver suggests
  firebase_storage: ^13.3.0      # updated
  cloud_firestore: ^6.3.0        # update if resolver suggests
  google_mlkit_face_detection: ^0.13.2
  flutter_local_notifications: ^21.0.0